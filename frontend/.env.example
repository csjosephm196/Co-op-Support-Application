# Local: leave unset (Vite proxies /api → backend in vite.config.ts).
# Vercel production: Render Web Service URL only — no /api suffix, no trailing slash.
# Example: https://csa-backend-xxxx.onrender.com
# VITE_API_URL=
